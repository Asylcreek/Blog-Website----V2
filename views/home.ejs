<%- include('./partials/header'); -%>
    <h1>Home</h1>
    <p>
        <%=homeStartingContent%>
    </p>

    <div>
        <% posts.forEach((el)=>{ %>
            <h2>
                <%= el.title %>
            </h2>
            <p>
                <%= el.shortContent %>
                    <a href="/posts/<%=el.slug %>">Read more</a>
            </p>
            <% }) %>
    </div>
    <%- include('./partials/footer'); -%>